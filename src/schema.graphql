type Query {
  books: [Book]
}

type Book {
  id: ID!
  title: String!
  author: String!
}

type AddBookMutationResponse {
  code: String
  success: Boolean
  message: String
  book: Book!
}

type Mutation {
  addBook(id: ID!, title: String!, author: String!): AddBookMutationResponse
}
